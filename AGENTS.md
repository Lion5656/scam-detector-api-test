## Code style guidelines
 
### 1. 實作前先產生計畫
 
在進行任何非瑣碎的程式碼變更前(新增功能、重構、涉及多個檔案的 bug 修復),
先以下列格式產出計畫,再開始實作:
 
1. **目標**:一句話描述要達成什麼
2. **影響範圍**:會修改／新增哪些檔案或模組
3. **拆分步驟**:條列式列出實作順序(每一步應是可獨立驗證的最小單位)
4. **風險／取捨**:是否有需要注意的邊界情況、相容性、效能影響
5. **驗證方式**:完成後如何確認正確(跑哪個測試、哪個指令)
計畫確認後才開始寫程式碼。若使用者未回覆確認,預設視為同意;
但遇到以下情況,必須停下來等待明確確認才可繼續:
 
- 會刪除或覆寫現有邏輯
- 影響資料庫 schema 或外部 API 合約
- 實作方式有多種合理選項,需要使用者取捨判斷
單一函式修正、拼字／格式修正等瑣碎變更可略過此流程,直接實作。
 
### 2. 巢狀與職責集中度
 
- 函式巢狀深度盡量不超過 2 層;超過時優先用 early return／guard clause 打平,
  而非直接視為「必須拆函式」的訊號
- 函式長度沒有硬性上限;只有當同一函式混雜多種職責
  (例如同時做 I/O 呼叫、資料轉換、商業邏輯判斷)時才需要拆分
- 一個檔案／模組只負責單一領域(例如「搜尋」「總結」「快取」分開放)

### 3. 函式拆分準則(避免過度拆分)
 
只有符合以下任一條件,才拆成獨立函式:
 
1. 邏輯會被複用 ≥ 2 次
2. 該段邏輯是獨立語意單元,可脫離上下文理解,適合單獨寫測試
3. 屬於不同關注點(I/O ／純運算／格式化／驗證)
不要拆分的情況:
 
- 只執行一次、線性依序執行的步驟,硬切成多個小函式
- 拆分後函式間需要傳遞大量參數或共享可變狀態(代表本質上是同一件事)
- 函式名稱與內容幾乎一比一翻譯,沒有抽象價值(例如 `add_one(x): return x + 1`)
拆分的目的是讓呼叫端讀起來像文件,而不是讓每個函式變短。
 
### 4. 命名與抽象層級
 
- 禁止使用空泛命名:`data`／`process`／`manager`／`util`,
  需具體對應業務語意(例如 `fetch_price_from_serp_api`,而非 `process_data`)。
  `handle` 僅允許用於最外層協調邏輯(見第 5 節動詞詞彙表),其餘情況視為空泛命名
- 不要引入未被使用的抽象層(interface／abstract class／factory／strategy),
  除非確實存在 ≥ 2 種具體實作
- 避免多層 wrapper／callback 包裝簡單邏輯

### 5. 命名長度控制
 
- 函式名應盡量控制在 2–4 個單字(動詞 + 名詞,必要時加修飾詞)
- 不要在函式名重複「模組名／類別名／檔案名」已表達的語意
  (例如在 `groq_summarizer.py` 內,函式命名為 `summarize()` 而非 `summarize_with_groq()`)
- 使用固定動詞詞彙表:`get`／`fetch`(讀取)、`list`(多筆)、`build`／`make`(組裝)、
  `validate`(驗證)、`normalize`(格式化)、`handle`(僅限最外層協調邏輯)
- 過多細節(如條件、延遲、重試策略)用參數表達,不要塞進函式名稱
- 只允許業界通用縮寫(`api`, `url`, `id`, `config`),業務核心詞彙不縮寫

### 6. 換行與行長度
 
- 單行程式碼上限為 100–120 字元(依語言慣例調整,Python 可用 100)
- 函式呼叫參數、import、字典／物件字面量等,只有在超過行長度上限時才拆成多行,
  未超過時保持單行
- 不要僅因為「參數較多」或「表達式較長」就主動換行,以實際字元數為唯一判斷依據
- 若專案已有 formatter 設定(如 black／prettier 的 line-length),
  以該設定為準,不要用更保守的預設值

### 7. 完成後自我審查
 
完成程式碼後,請自問:
 
1. 是否有函式混雜多種職責?→ 拆分(對照第 2、3 節)
2. 是否有只被呼叫一次、拆分後反而要跳來跳去才看得懂的函式?→ 合併回去
3. 是否有命名空泛、語意抽象、無法一眼看出用途的變數／函式?→ 重新命名(對照第 4、5 節)